module test;

Entity Math:
    has static method add: a: int, b: int -> int:
        return a + b;
    end method.
end Entity.

Entity App:
    has static method main -> int:
        x: int <- 10;
        y: int <- 20;
        # print: Math: add: x, y; # Static call not yet fully supported for mangling
        print: "Adding 10 and 20...";
        print: x + y;
        return 0;
    end method.
end Entity.
